name: Electron macOS release

on:
  push:
    tags:
      - 'electron-v*'

permissions:
  contents: write

jobs:
  package:
    name: Package macOS ${{ matrix.arch }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - arch: arm64
            runner: macos-14
          - arch: x64
            runner: macos-15-intel
    runs-on: ${{ matrix.runner }}
    timeout-minutes: 45
    steps:
      - uses: actions/checkout@v6
        with:
          persist-credentials: false

      - uses: pnpm/action-setup@v4
        with:
          version: 11.7.0

      - uses: actions/setup-node@v7
        with:
          node-version: 24
          cache: pnpm

      - name: Verify tag version
        run: |
          version=$(node -p 'require("./apps/electron/app/package.json").version')
          test "$GITHUB_REF_NAME" = "electron-v$version"

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Build web runtime
        run: pnpm run build

      - name: Package unsigned DMG
        run: node apps/electron/scripts/package-mac.mjs ${{ matrix.arch }}

      - name: Upload release assets
        uses: actions/upload-artifact@v4
        with:
          name: electron-macos-${{ matrix.arch }}
          if-no-files-found: error
          path: |
            apps/electron/dist/release/*.dmg
            apps/electron/dist/release/*.zip

  release:
    name: Publish GitHub release
    needs: package
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          persist-credentials: false

      - name: Download release assets
        uses: actions/download-artifact@v4
        with:
          pattern: electron-macos-*
          path: apps/electron/dist/release
          merge-multiple: true

      - name: Publish release assets
        uses: softprops/action-gh-release@v2
        with:
          # Publishing remains a deliberate maintainer action: validate the
          # two architecture-specific DMGs before exposing them to users.
          draft: true
          generate_release_notes: true
          files: |
            apps/electron/dist/release/*.dmg
            apps/electron/dist/release/*.zip
